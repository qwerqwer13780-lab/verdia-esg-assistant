.env
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store

# Markdown is generated from the fixed PDFs during the Railway build.
data/markdown/

# The PDFs are intentionally committed because they are the fixed knowledge base.
# The FAISS index/chunks are generated by Docker during deployment.
storage/*.index
storage/*.json
